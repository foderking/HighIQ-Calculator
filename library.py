# This module maps variable names to library functions so they can be used in the program

# log = # logarithm base 10
# b_log = # logarithm to specified base
# sqr = # square root
# addition
A = (lambda x, y: x + y)
M = (lambda x, y: x * y)
D = (lambda x, y: x / y)
d = (lambda x, y: x // y)
m = (lambda x, y: x % y)



